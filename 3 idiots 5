#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

   int x1,y1,x2,y2;
    scanf("%d%d%d%d",&x1,&y1,&x2,&y2);
    float r1=(x1+x2)/2.0;
    float r2=(y1+y2)/2.0;
    printf("Binoy's house is located at (%.1f,%.1f)",r1,r2);
    return 0;
}
