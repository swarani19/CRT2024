#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

   int a;
    scanf("%d",&a);
    int z=a%10;
    int h=10-z;
    if(h<=5)
    {
        printf("%d",(a+h));
    }
    else
    {
        printf("%d",(a-z));
    }
    return 0;
}
