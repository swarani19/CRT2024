#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
    int n,i;
    scanf("%d",&n);
    int res=0,tot=0;
    for(i=0;i<n;i++)
    {
        res=2+(res*10);
        tot=tot+res;
    }
    printf("%d",tot);   
    return 0;
}
