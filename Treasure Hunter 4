#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

     int n,p1,p2;
    scanf("%d%d%d",&n,&p1,&p2);
    int s1=(n*p1)/100;
    int s2=n-s1;
    int s3=(s2*p2)/100;
    int s4=(s2-s3)/3;
    printf("%d\n%d\n%d",s1,s3,s4);
    return 0;
}
